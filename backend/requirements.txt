# IntelliCode Review — Backend Dependencies
# Install: pip install -r requirements.txt

# API framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# ML — Core
numpy>=1.24.0
scikit-learn>=1.3.0
xgboost>=2.0.0

# ML — Deep learning
torch>=2.0.0
# torchvision  (uncomment if needed)

# ML — Transformers (CodeBERT)
transformers>=4.35.0
# datasets>=2.14.0   (needed for training only)
# evaluate>=0.4.0    (needed for training only)

# Code analysis
radon>=6.0.0          # cyclomatic/halstead metrics
astroid>=3.0.0        # advanced AST analysis

# Dataset mining (training only)
PyGithub>=2.1.0
pydriller>=2.5.0
pandas>=2.0.0

# Utilities
tqdm>=4.66.0
